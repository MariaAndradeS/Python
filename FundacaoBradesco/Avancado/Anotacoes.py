# widget = uma interface gráfica criada para facilitar o acesso a serviços e funções do celular, sem necessariamente rodar a aplicação para realizar determinada tarefa.
        #Widgets de layout --> Têm como objetivo posicionar outros widgets (column, row, stack etc).
        #Widgets de UI --> São aqueles visíveis ao usuário final (text, switch etc)
# Flutter, Dart, Swift = linguagens para mobile
# Kivy & PyQt -> pacote para aplicativos moveis